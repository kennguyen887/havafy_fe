export enum JobType {
  FULL_TIME = 'full-time',
  PART_TIME = 'part-time',
  CONTRACT = 'contract',
  TEMPORARY = 'temporary',
  OTHER = 'other',
  VOLUNTEER = 'volunteer',
  INTERNSHIP = 'internship',
}

export enum WorkplaceType {
  ON_SITE = 'on-site',
  HYBRID = 'hybrid',
  REMOTE = 'remote',
}

export enum WorkingShedule {
  MON_TO_FRI = 'mon-to-fri',
  MON_TO_SAT = 'mon-to-sat',
  FULL_WEEK = 'full-week',
}
